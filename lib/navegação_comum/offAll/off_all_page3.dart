import 'package:flutter/material.dart';

class OffAllPage3 extends StatelessWidget {
  const OffAllPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('offAll Page 3'),
      ),
      body: Container(),
    );
  }
}
